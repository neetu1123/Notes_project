
name: Linter

on:
  push:
    branches: [ "note_branch" ]
  pull_request:
    branches: [ "note_branch" ]
jobs:
  run-lint:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v3
        - name: Super-Linter
          uses: github/super-linter@v4.10.0
